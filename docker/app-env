#!/usr/bin/env bash
set -eo pipefail; [[ -n "$PLUSHU_TRACE" ]] && set -x

cat <<"EOF"
export RETHINKDB_HOST=$ADDON_RETHINKDB_LINK_PORT_28015_TCP_ADDR
export RETHINKDB_PORT=$ADDON_RETHINKDB_LINK_PORT_28015_TCP_PORT
export RDB_HOST=$ADDON_RETHINKDB_LINK_PORT_28015_TCP_ADDR
export RDB_PORT=$ADDON_RETHINKDB_LINK_PORT_28015_TCP_PORT
EOF
